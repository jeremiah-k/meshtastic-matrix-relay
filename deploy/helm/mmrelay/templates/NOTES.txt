Thank you for installing mmrelay!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

To verify the deployment:

  $ kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "mmrelay.name" . }},app.kubernetes.io/instance={{ .Release.Name }}

To run diagnostics inside the pod:

  $ POD_NAME=$(kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "mmrelay.name" . }},app.kubernetes.io/instance={{ .Release.Name }} -o jsonpath='{.items[0].metadata.name}')
  $ kubectl exec -n {{ .Release.Namespace }} $POD_NAME -- mmrelay doctor

DEPLOYMENT NOTES:
- Config is copied from {{ .Values.config.name }} {{ include "mmrelay.configSource" . }} to the PVC by the init container
- {{ if .Values.matrixAuth.enabled }}Matrix auth is enabled via {{ .Values.matrixAuth.secretName }} Secret{{ else }}Matrix auth is disabled - set matrixAuth.enabled=true and create the auth secret{{ end }}
- {{ if .Values.persistence.enabled }}Persistence is enabled - all data lives on the PVC{{ else }}WARNING: Persistence is disabled - all data is ephemeral{{ end }}

For more information on using this Helm chart, see:
https://github.com/jeremiah-k/meshtastic-matrix-relay/blob/main/docs/HELM.md
