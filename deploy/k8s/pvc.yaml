apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mmrelay-data
  namespace: mmrelay
  labels:
    app: mmrelay
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
