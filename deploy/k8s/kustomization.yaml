apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mmrelay
resources:
  - pvc.yaml
  - networkpolicy.yaml
  - deployment.yaml
images:
  - name: ghcr.io/jeremiah-k/mmrelay
    # NOTE: 'latest' is for development. For production, use an overlay
    # with a specific tag or digest (e.g., newTag: v1.3.0 or newDigest: sha256:...).
    newTag: latest
